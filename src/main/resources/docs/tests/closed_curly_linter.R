##Pattern: closed_curly_linter

# Pass
if (is.null(ylim)) {
  ylim <- c(0, 0.06)
}

##Warn: closed_curly_linter
if (is.null(ylim)) {
  ylim <- c(0, 0.06)}

##Warn: closed_curly_linter
if (is.null(ylim)) {ylim <- c(0, 0.06)}

